# Reverse complement all sequences from FASTA. Output JSON.
let seqs = read_fasta("data/sequences.fa")
let results = {}
seqs |> each(|s| {
    let rc = reverse_complement(s.seq)
    results[s.id] = str(rc)
})
let output = {sequences: results}
println(json_pretty(output))
