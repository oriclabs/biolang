# GC content per sequence, S. cerevisiae genome. Output JSON.
let seqs = read_fasta("real_data/yeast_genome.fa")
let result = {}
seqs |> each(|s| { result[s.id] = gc_content(s.seq) })
println(json_pretty({gc_per_sequence: result}))
