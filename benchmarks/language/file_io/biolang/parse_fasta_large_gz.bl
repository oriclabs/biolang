# File I/O: Parse large gzipped FASTA (Human chr22, ~10 MB compressed)
let records = fasta("data_real/human_chr22.fa.gz") |> collect()
println(f"Records: {len(records)}")
println(f"Total bp: {records |> map(|r| len(r.seq)) |> sum()}")
