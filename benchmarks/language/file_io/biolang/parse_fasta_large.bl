# File I/O: Parse large FASTA (Human chr22, ~51 MB)
let records = fasta("data_real/human_chr22.fa") |> collect()
println(f"Records: {len(records)}")
println(f"Total bp: {records |> map(|r| len(r.seq)) |> sum()}")
