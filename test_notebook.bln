# Converted from: test_notebook.Rmd

---
title: "DESeq2 Analysis"
output: html_document
---

# Differential Expression Analysis

This notebook performs differential expression using DESeq2.

```r setup
library(DESeq2)
library(dplyr)
```


## Load data

```r load
counts <- read.csv("counts.csv", row.names = 1)
metadata <- read.csv("metadata.csv")
```


## Run DESeq2

```r deseq
dds <- DESeqDataSetFromMatrix(countData = counts, colData = metadata, design = ~condition)
dds <- DESeq(dds)
res <- results(dds, contrast = c("condition", "treated", "control"))
```


## Filter results

```r filter
sig <- as.data.frame(res) %>%
  filter(padj < 0.05) %>%
  arrange(padj)
print(head(sig))
```


This chunk is skipped:

```r eval=FALSE
# expensive step
run_long_analysis()
```

