[package]
name = "motif"
version = "0.1.0"
description = "Transcription factor motif analysis: IUPAC scanning, PWM construction, motif enrichment"
authors = ["BioLang"]

[dependencies]
statistics = { path = "../statistics" }

[lib]
entry = "src/mod.bl"
