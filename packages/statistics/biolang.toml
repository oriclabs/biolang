[package]
name = "statistics"
version = "0.1.0"
description = "Explainable exploratory statistics, visual guidance, tests, resampling, and multiple-testing correction"
authors = ["BioLang"]

[lib]
entry = "src/mod.bl"
