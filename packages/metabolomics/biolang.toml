[package]
name = "metabolomics"
version = "0.1.0"
description = "LC-MS metabolomics: m/z matching, isotope correction, feature grouping, pathway enrichment"
authors = ["BioLang"]

[dependencies]
statistics = { path = "../statistics" }

[lib]
entry = "src/mod.bl"
