[package]
name = "atac"
version = "0.1.0"
description = "ATAC-seq quality control: fragment size distribution, NFR enrichment, TSS enrichment, nucleosome fractions"
authors = ["BioLang"]

[lib]
entry = "src/mod.bl"
